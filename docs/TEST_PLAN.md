# 点云测量工具 2.2 测试计划

## 1. 测试原则与数据集

每项功能至少覆盖：正常路径、边界值、无效输入、退化数据、状态切换、工程恢复、导出回读和旧功能回归。数值算法使用可手算的合成点云断言；GUI 使用 QtTest 驱动真实控件；大数据性能使用实际彩色地图自动回归，关键布局同时保存截图做视觉复核。

固定数据集：

- `xyz_no_color_ascii.pcd`：12 个无颜色点，验证高度着色与小数据边界。
- `rgba_ascii.pcd`：12 个 RGBA 点，验证颜色转换。
- `invalid_points_ascii.pcd`：包含 NaN/Inf，验证清理与计数。
- 运行时生成的 ASCII/Binary PLY、MAPV0001 头和 OLX 15/16 B 点记录：验证多格式与损坏边界。
- 合成平面/斜面/圆/方体/离群点/平移点云：测试程序运行时构造，答案精确已知。
- `odin1_colored_map_20260902_111205.pcd`：1,514,634 点真实回归地图。
- `map_20260902_103830.bin`：277,628,979 字节 MAPV0001 v5 真实设备地图。
- 真实 Odin1 临时采集 OLX：只在真机验收期间生成，读回后删除。
- 运行时生成的 ROS1 bag、ROS2 SQLite/压缩 SQLite/MCAP：包含规则时序、长间隙、倒退、QoS、TF 和传感器故障注入。
- `static_20260716_typed`：2,933 条真实 ROS2 Livox/IMU 消息，用对应工作空间验证自定义消息与字段级分析。

通过标准：自动测试 0 失败；数值误差在每个用例声明的容差内；真实地图点数与推荐尺寸不发生非预期变化；导出文件可重新读取；失败路径不崩溃且不破坏当前工程状态。

## 2. 文件加载与工程

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| FILE-01 | ASCII / binary / compressed PCD | 均成功加载，字段和编码正确 |
| FILE-02 | RGB、RGBA、XYZ-only | 保留颜色；XYZ-only 自动高度着色 |
| FILE-03 | NaN、Inf、空云、少于 3 个有效点 | 非有限点准确计数；不能分析时给出明确错误 |
| FILE-04 | 缺失 XYZ、错误扩展名、截断文件、不存在路径 | 拒绝加载，不替换当前有效点云 |
| FILE-05 | 中文、空格、逗号和超长路径 | 读取、拖放和启动参数均可打开 |
| FILE-06 | 连续快速加载两个文件 | 忙碌期间阻止重入；完成后状态与路径一致 |
| FILE-07 | 最近文件中存在/失效路径 | 存在项可打开，失效项自动剔除 |
| FILE-08 | v1–v6、v7 与 `.odinpcd` 工程 | 旧测量兼容；新状态完整恢复 |
| FILE-09 | PCD 移动但工程相对路径有效 | 使用相对路径恢复 |
| FILE-10 | 工程 JSON 损坏、引用 PCD 丢失 | 明确提示，不崩溃 |
| FILE-11 | 自动恢复的接受、拒绝、损坏文件 | 接受时恢复并清理；拒绝时删除；损坏时不留异常状态 |
| FILE-12 | 基准云/第二云/派生区域移动或丢失 | 相对路径可恢复；可选组件丢失时安全降级 |
| FILE-13 | PLY/MAP BIN/OLX 点击、拖放、最近文件和启动参数 | 自动识别来源并进入同一测量流程 |
| FILE-14 | `.bin` 不是 MAPV0001 或选择 OLX 配套 BIN | 拒绝并指向 `.olx` 主文件，不尝试误解码 |
| FILE-15 | 支持格式作为第二云 | 统一加载、ICP/差异和导出流程不依赖源扩展名 |

## 3. 点云统计、LOD 与查看

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| VIEW-01 | 总数、有效/无效、颜色比例、文件大小 | 与 PCD 头和独立计算一致 |
| VIEW-02 | 最低/最高点、质心、XYZ 范围、点间距 | 合成点云答案准确；重复点不产生错误 |
| VIEW-03 | 原始 AABB 与稳健主方向框 | 旋转矩形和带离群点数据尺寸正确 |
| VIEW-04 | 球形/方形导致 PCA 特征值接近 | 结果有限、稳定，不出现 NaN |
| VIEW-05 | 最大显示点数为 1、边界值和大于总数 | 不除零；显示点数不超限；完整统计不变 |
| VIEW-06 | RGB/高程/纯色/差异模式反复切换 | 云始终存在，颜色模式状态一致 |
| VIEW-07 | 黑/灰/自定义背景、点大小 1–10 | 渲染属性准确 |
| VIEW-08 | 透视/正交、五种视角、适应窗口 | 相机有限且点云可见 |
| VIEW-09 | 坐标轴、网格、尺寸框反复开关 | 不重复创建残留 shape |
| VIEW-10 | 窗口最小尺寸、高 DPI、滚动侧栏 | 控件不重叠，键盘焦点清楚 |
| VIEW-11 | 测量文字标签开关和大/小场景 | 标签随场景限幅缩放，关闭后只隐藏文字不隐藏几何 |

## 4. 选区与裁剪

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| SEL-01 | 鼠标 XY 两点正序/反序 | 自动归一化边界，点数一致 |
| SEL-02 | 精确 XYZ 框含边界点 | 边界点计入，Z 范围生效 |
| SEL-03 | 反选 3D 框 | 结果与全集减框内点一致 |
| SEL-04 | 零宽、超范围、NaN、min>max | 校验或自动归一化，不崩溃 |
| SEL-05 | 多边形顺/逆时针、凹多边形、边界点 | 选点结果一致，凹区正确 |
| SEL-06 | 自交、共线、少于 3 点多边形 | 拒绝并解释原因 |
| SEL-07 | 多边形反选和 Z 限制 | 集合逻辑准确 |
| SEL-08 | 空区域和少于 3 点区域 | 不替换当前区域，给出提示 |
| SEL-09 | 局部统计与导出回读 | 数量、边界、颜色与导出 PCD 一致 |
| SEL-10 | 恢复完整点云 | 视图、吸附树和总统计恢复 |
| SEL-11 | 保存/恢复框选和套索状态 | 工程重开后选择结果一致 |

## 5. 测量与记录管理

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| MEAS-01 | 单点、线段、折线 | 坐标、3D/水平距离和累计长度准确 |
| MEAS-02 | 水平、垂直、零长度、负高差 | 坡度、角度和无限坡度显示正确 |
| MEAS-03 | 0°、90°、180°及重合点角度 | 正常值准确，重合点拒绝 |
| MEAS-04 | 水平、倾斜、凹、多点面积 | 拟合面积、投影面积、周长和 RMS 准确 |
| MEAS-05 | 共线、重复点、自交面积 | 无效几何拒绝或明确标识 |
| MEAS-06 | 正交分解八个方向 | ΔX/Y/Z 符号和直线距离正确 |
| MEAS-07 | 点在平面上/两侧、垂直平面、平面点共线 | 绝对/有向距离和法向正确；退化拒绝 |
| MEAS-08 | 水平/垂直/倾斜圆及近共线点 | 圆心、法向、半径、直径、周长正确 |
| MEAS-09 | LOD 画面选点 | 坐标吸附到完整云真实点 |
| MEAS-10 | m/cm/mm 往返 | 距离乘 100/1000，面积乘平方，源值不漂移 |
| MEAS-11 | 新建、取消节点、完成条件 | 固定点数自动完成；折线/面积最低节点限制正确 |
| MEAS-12 | 名称/分组/备注/颜色/显隐/坐标编辑 | 重新计算和渲染正确，非法输入不能保存 |
| MEAS-13 | 搜索、类型/分组筛选、组显隐 | 表格与三维对象同步，不删除记录 |
| MEAS-14 | 复制、删除、全部清除 | ID 唯一；选择行稳定 |
| MEAS-15 | 100 步撤销/重做、分支编辑 | 上限生效；新操作清空 redo；状态精确恢复 |
| MEAS-16 | JSON 导入重复 ID、未知类型、无效点 | 安全重编号或跳过并汇总错误 |
| MEAS-17 | CSV/JSON 特殊字符与回读 | 中文、逗号、引号、换行不破坏格式 |

## 6. 区域分析

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| ANA-01 | 水平/倾斜/垂直平面拟合 | 中心、法向、坡度、方位正确 |
| ANA-02 | 平面噪声和单个大离群点 | RMS/最大残差反映噪声；结果有限 |
| ANA-03 | 少于 3 点、共线点 | 平面分析拒绝 |
| ANA-04 | 密度、Z 均值/标准差、最近邻 | 合成答案在容差内 |
| ANA-05 | 高程直方图常量 Z、空集、1/最大箱数 | 不除零；计数总和等于点数 |
| ANA-06 | 图表空集、常量轴、NaN 断点/NaN 柱 | 可安全绘制，非有限值不进入绘制坐标 |
| PROF-01 | 单段、折线、折返路径 | 里程和最近线段归属正确 |
| PROF-02 | 宽度/分箱边界点、空箱 | 边界规则一致，空箱明确 |
| PROF-03 | 零长度段、路径少于 2 点、宽度≤0 | 无效输入拒绝 |
| PROF-04 | 剖面 CSV 与图像 | 行数、里程、min/mean/max 和截图一致 |
| VOL-01 | 水平矩形高于/低于基准 | 高/低/净体积可手算一致 |
| VOL-02 | 同时挖填、凹边界、不同格网尺寸 | 符号和覆盖面积正确，收敛趋势合理 |
| VOL-03 | 空栅格、单点栅格、基准等于表面 | 无数据比例正确，零体积稳定 |
| VOL-04 | 非法格网、退化/自交边界 | 拒绝计算 |
| CLEAN-01 | 网格点加孤立离群点 | SOR 移除离群点且主体保留 |
| CLEAN-02 | 极小云、meanK 超限、阈值≤0 | 参数归一化或拒绝，不崩溃 |
| PLANE-01 | 主平面加非平面点 | RANSAC 内点率、方程和分组正确 |
| PLANE-02 | 阈值边界、无主平面、垂直平面 | 输出稳定或明确失败 |

## 7. 双点云配准与差异

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| CMP-01 | 完全相同云直接比较 | 所有距离近零 |
| CMP-02 | 已知平移/旋转云 ICP | 收敛，逆变换在容差内 |
| CMP-03 | 无重叠、少于 3 点、尺度不一致 | 不误报成功，保留当前状态 |
| CMP-04 | 体素 0/极大、迭代 1/上限、对应距离边界 | 参数校验，算法不崩溃 |
| CMP-05 | ICP 关闭 | 变换为单位阵，距离反映原坐标差 |
| CMP-06 | 最近邻均值/RMSE/中位/P95/max | 与独立排序计算一致 |
| CMP-07 | 阈值 0、等于距离、极大 | 超阈值计数边界定义一致 |
| CMP-08 | 热力图距离 0/阈值/超阈值 | 颜色端点和夹取正确 |
| CMP-09 | 第二云含 NaN/颜色/大云 LOD | 清理、颜色和显示限制正确 |
| CMP-10 | 导出对齐 PCD/CSV/JSON 回读 | 点数、坐标、矩阵、距离一致 |
| CMP-11 | 工程保存后原文件移动/丢失 | 相对路径恢复或明确降级 |
| CMP-12 | 清除对比 | 第二云和热力图 shape 清除，原色恢复 |

## 8. 坐标变换

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| XFM-01 | 平移、Z 旋转、统一缩放及组合 | 4×4 矩阵和已知点坐标准确 |
| XFM-02 | 角度 -360/360/大值、尺度极小/负/零 | 角度归一；非法尺度拒绝 |
| XFM-03 | 预览、取消、应用、撤销 | 源云仅在应用后变化且可恢复 |
| XFM-04 | 显示原点偏移 | UI 坐标改变但距离不变、源 PCD 不变 |
| XFM-05 | 导出变换云和矩阵回读 | 坐标、颜色、点数和矩阵一致 |
| XFM-06 | 变换后测量/裁剪/对比 | 所有空间操作使用同一坐标系 |

## 9. 输出、报告与错误恢复

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| OUT-01 | PNG、PCD、CSV、JSON、PDF 正常写入 | 文件存在、非空并可回读 |
| OUT-02 | 无权限、目录路径、磁盘写入失败 | 报错且不留下伪成功临时文件 |
| OUT-03 | PDF 无测量/大量测量/长中文备注 | 分页自然，无截断和乱码 |
| OUT-04 | 报告截图、统计、分析和对比摘要 | 与当前工程状态一致 |
| OUT-05 | 保存过程中源 PCD 不变 | 哈希或大小保持一致 |
| OUT-06 | 已有目标与写入中途失败 | 采用临时文件提交，旧文件不被半成品替换 |
| ERR-01 | 分析异常后按钮、光标、进度条 | 全部恢复到可操作状态 |
| ERR-02 | 反复打开/关闭对话框和主窗口 | 无崩溃、无残留后台任务 |

## 10. 安装、启动与打包

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| PACK-01 | `build.sh` / `run.sh` / `install_desktop.sh` / 验收脚本语法 | `bash -n` 全部通过 |
| PACK-02 | CLI 缺少参数、显示上限 0、有效参数 | 无效参数非零退出；有效输出可解析 JSON |
| PACK-03 | 应用以 PCD 和 `.pcdmeasure` 启动参数运行 | 真实 GUI 载入完成并正常退出 |
| PACK-04 | 隔离桌面/应用菜单目录安装 | 两个 launcher 可执行，Exec/Icon/名称/描述正确 |
| PACK-05 | 产品名和 README 安装指南 | 名称不含传感器品牌，下载后步骤齐全 |
| PACK-06 | 并行编译值 0/非数字及 32 核低内存主机 | 无效值拒绝；默认最多 2 任务防止 OOM |

## 11. ROS Bag 回放与离线诊断

### 11.1 格式、索引与依赖

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| BAG-FMT-01 | ROS1 `.bag` 规则录制 | 自动识别 ROS1，读取内嵌类型、消息数和时间 |
| BAG-FMT-02 | ROS2 SQLite 目录、单独 `.db3` | 两种入口均识别；相邻 metadata 存在时按完整 bag 分析 |
| BAG-FMT-03 | ROS2 多分片 SQLite，选目录或任一分片 | 自动展开 metadata 中全部分片，不漏计 |
| BAG-FMT-04 | ROS2 MCAP 目录、独立 `.mcap`、多分片目录 | 均可读取并合并消息 |
| BAG-FMT-05 | SQLite 文件级/消息级 zstd 压缩 | 使用可选读取器透明解压，报告保存压缩模式 |
| BAG-FMT-06 | 空 bag、单消息话题、无 topics metadata | 空 bag 为严重问题；单消息明确不可计算频率；不崩溃 |
| BAG-FMT-07 | metadata 消息数错误、引用分片丢失 | 输出一致性告警或稳定错误，并建议备份后 `ros2 bag reindex` |
| BAG-FMT-08 | 损坏 SQLite、ROS1、MCAP、截断存储 | 非零失败且给出用户可读错误，不输出 Python traceback |
| BAG-FMT-09 | 不存在路径、错误扩展名、普通目录 | 输入按钮禁用或明确拒绝 |
| BAG-FMT-10 | 中文、空格、分号、方括号和长路径 | 作为一个参数安全传递，不发生命令拼接 |
| BAG-FMT-11 | 未安装 `rosbags` / PyYAML / ROS 消息类型 | 说明安装方法；能做的存储层分析继续完成 |
| BAG-FMT-12 | 百万级消息与每话题抽样上限 | 时间样本和字段样本有界，结果可复现，不随消息数线性耗尽内存 |

### 11.2 频率、丢包与时间戳

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| BAG-TIME-01 | 20/100/200 Hz 等周期消息 | 平均/中位频率、周期和抖动与手算一致 |
| BAG-TIME-02 | 小幅随机周期扰动 | CV、标准差、P95/P99 周期准确，超过阈值才告警 |
| BAG-TIME-03 | 单个或多个长间隙 | 最大间隙、间隙数和估算缺失准确；长停顿不污染调度抖动 |
| BAG-TIME-04 | 低频控制/IMU/里程计/激光/TF | 按内置或用户阈值提示未达标，普通低频状态话题不误报控制故障 |
| BAG-TIME-05 | 相同存储时间戳超过 1% | 输出批量同时间戳警告和比例 |
| BAG-TIME-06 | 存储时间戳回退 | 严重时序错乱并建议检查 NTP/PTP 与时间源 |
| BAG-TIME-07 | `header.stamp` 缺失或为零超过 5% | 输出字段级时间戳警告 |
| BAG-TIME-08 | `header.stamp` 回退 | 输出严重 header 时序错乱 |
| BAG-TIME-09 | header 与存储时间偏差超过阈值 | 输出中位/P95/最大延迟与时钟同步建议 |
| BAG-TIME-10 | 规则 header、存储写入成批抖动 | 有足够字段样本时用 header 作为主时基，保留 storage_timing 供对照 |
| BAG-TIME-11 | 字段抽样、部分解码失败 | 样本周期按抽样倍率还原；精确消息总数不被抽样数替代 |
| BAG-TIME-12 | 非法阈值、抽样数为负或过小 | 启动前拒绝并指出具体参数 |
| BAG-TIME-13 | `/clock` + 仿真纪元 header + Unix 存储时间 | 识别为不同时间基准并提示 `use_sim_time`，不误报万亿毫秒传输延迟 |
| BAG-TIME-14 | 事件话题长停顿、聚合 `/tf` 混合多种发布周期 | 不套用恒定周期流的丢包/抖动严重度，保留适用的专项检查 |
| BAG-TIME-15 | 5 Hz 基线附近的纳秒取整误差 | 显示 5.00 Hz 且在 0.5% 容差内不误报低频 |

### 11.3 QoS 与 TF

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| BAG-QOS-01 | 记录 RELIABLE/BEST_EFFORT 与 durability | 表格显示录制策略，不把单一有效策略误报为不匹配 |
| BAG-QOS-02 | 同话题保存多种 reliability/durability | 标记混合配置和回放兼容风险 |
| BAG-QOS-03 | `/tf_static` 为 VOLATILE | 标记应复核 TRANSIENT_LOCAL |
| BAG-QOS-04 | bag 没有 offered QoS | 提示无法离线确认订阅端兼容性，不伪造确定结论 |
| BAG-TF-01 | 正常单树动态/静态 TF | 图连通、无环、每 child 单父节点，无异常告警 |
| BAG-TF-02 | 多父节点、图环、多个连通分量 | 分别输出严重/警告并列出相关 frame |
| BAG-TF-03 | 空 frame、NaN/Inf、零四元数 | 计入无效变换，不进入有效边 |
| BAG-TF-04 | 非单位四元数 | 输出归一化警告和 P95 模长误差 |
| BAG-TF-05 | 静态 TF 在录制期间变化 | 严重告警，建议排查重复发布者和外参 |
| BAG-TF-06 | 短 bag 仅两帧的大平移/旋转 | 仍按基础阈值识别跳变，不因自适应中位数漏报 |
| BAG-TF-07 | 长 bag 正常移动后单次跳变 | 使用稳健自适应阈值识别平移/旋转不连续 |
| BAG-TF-08 | TF 更新断档、时间回退、速度尖峰 | 三种模式分别报告；证据包含间隔或速度 |
| BAG-TF-09 | 正常机器人持续运动 | 记录净位移和累计路径；报告说明位移本身不等于故障 |
| BAG-TF-10 | bag 无 `/tf` 或消息不可解码 | 明确说明 TF 诊断不可用及如何补录/加载类型 |
| BAG-TF-11 | 动态 TF 被均匀抽样 / 完整解析 | 抽样时不伪造断档并显示覆盖数；完整解析时按逐边真实时间检查断档 |

### 11.4 传感器与硬件模式

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| BAG-SEN-01 | IMU 正常、NaN、饱和、非单位姿态、短期剧变 | 无效、饱和、四元数和噪声模式分别统计 |
| BAG-SEN-02 | LaserScan 空数组、Inf/越界比例高、重复帧 | 空帧、无效距离比例和冻结模式报告 |
| BAG-SEN-03 | PointCloud2 空云、width/height/step/data 不一致 | 空帧或结构异常报告，正常云不误报 |
| BAG-SEN-04 | Livox CustomMsg 正常、point_num 不一致、无效点 | 识别 Livox 角色，统计点数、距离、反射率和结构异常 |
| BAG-SEN-05 | Image/CompressedImage 空载荷、尺寸不符、重复内容 | 空帧、结构异常和冻结模式报告 |
| BAG-SEN-06 | Odometry 正常轨迹和大位置跳变 | 记录步长并提示可疑瞬移 |
| BAG-SEN-07 | JointState 名称/位置/速度长度不一致 | 输出结构异常，不越界 |
| BAG-SEN-08 | NavSatFix NO_FIX、非有限坐标、高协方差 | NO_FIX 和无效值进入报告 |
| BAG-SEN-09 | BatteryState 低电量、零/无效电压、60/80 °C 边界 | 低电量、过温严重度和无效值准确 |
| BAG-SEN-10 | DiagnosticArray WARN/ERROR 重复出现 | 聚合设备名、级别、消息和次数；ERROR 为严重 |
| BAG-SEN-11 | 话题仍有频率但载荷连续相同 ≥10 帧 | 标记硬件/驱动疑似冻结 |
| BAG-SEN-12 | 自定义消息工作空间未加载/加载正确 | 前者安全降级并给建议；后者字段覆盖率提高且无解码警告 |
| BAG-SEN-13 | 机器人静止时 Odometry/JointState 长时间不变 | 降为低置信提示，不直接判定硬件冻结 |

### 11.5 报告、界面与回放

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| BAG-REP-01 | 正常、警告、严重问题组合 | 健康分、计数、状态和健康轨颜色一致 |
| BAG-REP-02 | 同一问题重复触发 | 按稳定 issue ID 去重，保留最高严重度 |
| BAG-REP-03 | JSON/HTML 导出与回读 | schema/version/证据/建议/边界完整，JSON 不含 NaN |
| BAG-REP-04 | 话题与路径包含 HTML 特殊字符 | HTML 完整转义，不形成脚本或标签注入 |
| BAG-REP-05 | 输出目录不存在、已有目标、写入失败 | 自动建目录并原子替换；失败不留下半报告 |
| BAG-REP-06 | 进度回调 | 从 1 单调到 100；GUI 忙碌态结束后恢复 |
| BAG-GUI-01 | 左侧导航、启动参数和主窗口拖放 bag | 原位切换到内嵌 Bag 工作台，路径、格式正确且不触发点云加载 |
| BAG-GUI-02 | 选择文件/ROS2 目录、清空和不支持路径 | 按钮状态与输入有效性同步 |
| BAG-GUI-03 | setup.bash 向上自动搜索、手选、失效 | 自动匹配最近工作空间；失效脚本阻止启动并提示 |
| BAG-GUI-04 | 诊断完成后的四个页签、表格和报告按钮 | 数据行数、数值、严重色和启用状态与 JSON 一致 |
| BAG-GUI-05 | 1100×700、1540×920、高 DPI 与长中文 | 内嵌页可滚动、无关键遮挡，深色对比度和按钮状态清楚 |
| BAG-GUI-06 | HTML 默认文件关联损坏、浏览器可用 | “打开完整报告”绕过错误关联，向默认/备用浏览器传入单个本地 `file://` URL；失败时给出可操作提示 |
| BAG-GUI-07 | 点云/采集/Bag 工作台反复切换 | 不创建重复顶层窗口；上下文命令栏只在点云页显示，运行状态保留 |
| BAG-PLAY-01 | ROS1/ROS2 回放；0.05–20 倍率 | 调用对应命令，倍率作为独立参数传递 |
| BAG-PLAY-02 | 循环、`/clock` 开关的四种组合 | 只添加所选参数；ROS1/ROS2 参数形式正确 |
| BAG-PLAY-03 | 自定义 setup、中文/空格路径 | 环境成功加载，路径不被拆分 |
| BAG-PLAY-04 | 暂停、继续、停止和自然结束 | SIGSTOP/SIGCONT/TERM 状态与按钮文字一致，无残留进程 |
| BAG-PLAY-05 | ROS 命令、插件、消息类型或 bag 不存在 | 日志显示原始失败原因；诊断功能仍可单独使用 |
| BAG-PLAY-06 | 回放期间再次回放、修改倍率、切换工作台或关闭主窗口 | 防止重入；切换后继续运行且状态保留；关闭程序时回收子进程 |
| BAG-SAFE-01 | 诊断前后计算 bag 文件大小/哈希 | 原始 bag 和 metadata 不被修改 |
| BAG-SAFE-02 | 仅诊断且 ROS 图中无节点 | 不调用 `ros2 bag play`，不发布任何话题 |

## 12. 多格式、OLX 与设备采集

### 12.1 PLY 与 MAP BIN

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| FMT-PLY-01 | ASCII PLY XYZ、XYZRGB | 点数、坐标、颜色、字段和边界正确 |
| FMT-PLY-02 | 二进制 PLY、空顶点、缺少 XYZ、截断 | 有效文件读取；无效文件给出明确错误 |
| FMT-MAP-01 | `MAPV0001` v5 真实地图 | 官方转换器成功，XYZ 点数与独立转换一致 |
| FMT-MAP-02 | 错误魔数、太短、未知版本、只读路径 | 转换前拒绝，源文件和现有工作状态不变 |
| FMT-MAP-03 | 转换器不存在、无执行权限、超时、非零退出、无输出 | 提示安装/权限/原始 stderr，不留下伪 PLY |
| FMT-MAP-04 | amd64/arm64 下载选择与 SHA-256 | 只接受支持架构和固定哈希，篡改下载被拒绝 |
| FMT-MAP-05 | 临时 PLY 生命周期 | 成功或失败后都自动清理，不污染地图目录 |

### 12.2 OLX 解析

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| OLX-READ-01 | 15 B XYZRGB 与 16 B XYZRGBA | 自动判别步长，坐标、颜色、帧数和点数准确 |
| OLX-READ-02 | 单帧、三帧、零点帧和大点数帧 | 索引与累计偏移正确，零点帧不导致崩溃 |
| OLX-READ-03 | 帧 ID 不连续、重复、时间相同 | 文件仍可读并保留原始 ID/时间，不伪造帧 |
| OLX-READ-04 | 时间回退、NaN/Inf 时间戳 | 时间回退给出警告；非有限时间拒绝 |
| OLX-READ-05 | 点坐标 NaN/Inf | 无效点计数准确，不进入显示和计算 |
| OLX-READ-06 | 截断帧头、截断点、尾部垃圾、伪造超大点数 | 严格边界检查，报告具体偏移，不越界/不分配异常内存 |
| OLX-READ-07 | 15/16 B 都可整除的歧义夹具 | 用完整结构和结束偏移择优，不因首帧误判 |
| OLX-READ-08 | 空文件、普通二进制、错误扩展名 | 稳定失败，当前已加载点云保持可用 |
| OLX-READ-09 | `OdinPose.bin` 40 B 记录完整/截断 | 完整记录计数，残尾警告且点云仍可用 |
| OLX-READ-10 | `OdinImage.bin` 内嵌 JPEG/PNG 完整/截断 | 建立偏移索引并按需解码；坏帧只影响预览 |
| OLX-READ-11 | `image/*.jpg/png` 外部帧 | 自然排序并与内嵌帧合并，坏图跳过并警告 |
| OLX-READ-12 | 位姿、相机和标定全部缺失 | OLX 点云正常打开，缺失项明确显示为警告 |
| OLX-READ-13 | 中文、空格、超长会话路径 | 主文件和配套文件均可读取 |
| OLX-READ-14 | 百万级点/数百帧 | 全量统计有界完成，显示 LOD 不改变结果 |

### 12.3 OLX 时间轴与工程

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| OLX-GUI-01 | 单帧/累计切换 | 当前显示点数严格等于所选范围 |
| OLX-GUI-02 | 累计全部帧、窗口 1/2/110/200/大于总帧 | 起止帧夹取正确，不越界 |
| OLX-GUI-03 | 滑块首帧/中间/末帧和键盘改变 | 标签、相对时间、点云与相机同步 |
| OLX-GUI-04 | 0.25/0.5/1/2/4 倍播放、末尾停止 | 定时推进正确，末帧自动停且按钮恢复 |
| OLX-GUI-05 | RGB/高程/纯色/帧着色反复切换 | 点数不变，帧颜色可区分，无残留 actor |
| OLX-GUI-06 | 相机预览开/关、无图、坏图、时间不完全对齐 | 最近时间帧可见；不可用时显示清楚且不阻塞点云 |
| OLX-GUI-07 | 时间轴状态保存工程并重开 | 当前帧、模式、窗口、倍率、相机和视角恢复 |
| OLX-GUI-08 | 播放中加载新文件、关闭窗口或打开工程 | 先停止计时器，不访问已释放云或相机数据 |
| OLX-GUI-09 | 1100×700、1540×920 与高 DPI | 控制轨不遮挡，侧栏和相机窗可用 |

### 12.4 ROS2 OLX 录制器与设备面板

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| OLX-REC-01 | 小端/大端 PointCloud2 XYZRGB/RGBA | 统一写出精确 16 B XYZRGBA，颜色位不串位 |
| OLX-REC-02 | intensity、无颜色、额外字段、行填充 | intensity 转灰度，无颜色用中性色，正确使用 point/row step |
| OLX-REC-03 | 空帧、NaN/Inf、结构长度不足 | 跳过无效点或记录错误，不写损坏帧 |
| OLX-REC-04 | 位姿正常、非有限位置/四元数 | 正常记录为固定 40 B；非法消息不污染文件 |
| OLX-REC-05 | CompressedImage JPEG/PNG 与空数据 | 写入 `OdinImage.bin` 索引；空图跳过 |
| OLX-REC-06 | 无位姿/无相机选项 | 创建可读零长度配套文件，主 OLX 完整 |
| OLX-REC-07 | 按帧上限、用户停止、Ctrl+C、ROS shutdown | 刷新并关闭全部文件，已完成帧可重新打开 |
| OLX-REC-08 | 会话名空白、路径穿越、重名目录、不可写目录 | 启动前拒绝或创建唯一安全目录 |
| OLX-REC-09 | 每秒状态与最终状态 | 帧/点/位姿/图像/错误计数和路径准确 |
| OLX-REC-10 | ROS 环境脚本访问未定义变量 | wrapper 在 source 后启用 nounset，Humble 可正常启动 |
| OLX-REC-11 | 实时预览原子文件、抽样上限和连续覆盖 | 文件始终是完整 `PCPV0001` 帧；最多 60,000 点，XYZRGBA 与写盘帧一致 |
| DEVICE-01 | USB 存在/不存在/无权限 | 状态颜色和建议准确，不因探测阻塞界面 |
| DEVICE-02 | 工作空间有效/无 setup/路径含空格 | 只对有效环境启用驱动和录制，参数不被拆分 |
| DEVICE-03 | 驱动未运行/已运行/启动失败/自然退出 | 不重复启动；日志和按钮状态正确；子进程可回收 |
| DEVICE-04 | SLAM、DTOF、位姿、相机话题组合 | 命令参数与选择一致；缺少可选话题仍可录点云 |
| DEVICE-05 | 录制中切换工作台或关闭主窗口 | 切换后继续录制并保留预览状态；关闭程序时安全完成并回收子进程 |
| DEVICE-06 | 完成后自动打开 | 只在退出码 0 且 OLX 存在时发出打开请求 |
| DEVICE-07 | 驱动配置只读一致性 | 采集前后 YAML 哈希相同，不为录制偷偷改设备参数 |
| DEVICE-08 | 实时预览首帧/连续帧/坏文件/大帧 | 状态显示帧号、显示点数和刷新率；坏帧不替换上一画面；大帧限量显示 |
| DEVICE-09 | 实时画面旋转、滚轮缩放、双击复位 | 交互流畅且只改变显示，不影响 OLX 点数、颜色和文件内容 |

## 13. 实际地图、真实 Bag 与人工视觉验收

| ID | 测试点 | 预期结果 |
| --- | --- | --- |
| REAL-01 | 完整加载基准地图 | 1,514,634 有效点，RGB 非黑点 1,511,348 |
| REAL-02 | 稳健尺寸 | 19.346 × 11.344 × 3.953 m；3D 对角线 22.772 m |
| REAL-03 | 强制显示≤100,000 点 | 实际显示≤100,000，完整统计不变 |
| REAL-04 | 真实地图上同时渲染八种测量 | 标记清楚、标签不闪烁，自动保存组合截图 |
| REAL-05 | 框选/套索/剖面/体积/平面/清理 | 操作响应与结果展示完整 |
| REAL-06 | 第二云 ICP 和差异着色 | 交互期间界面不假死，图例可读 |
| REAL-07 | 1100×700、1540×920、1.5× 高 DPI | 主视图可用，侧栏可滚动，按钮无截字 |
| REAL-08 | 深空蓝视觉、焦点、禁用/危险状态 | 层级一致，测量橙与差异热力图可区分 |
| REAL-09 | 完整工程保存、关闭、重开 | 相机、选区、测量、分析和对比全部恢复 |
| REAL-10 | 一键完整验收脚本 | 所有自动用例 0 失败并打印关键基准值 |
| REAL-MAP-01 | 真实 `MAPV0001` v5 BIN | 972,514 点；推荐尺寸约 19.208 × 12.640 × 3.895 m |
| REAL-MAP-02 | MAP BIN 源文件只读一致性 | 转换和分析前后大小/哈希不变，临时 PLY 已清理 |
| REAL-OLX-01 | 真实 Odin1 `/odin1/cloud_slam` 录制 30 帧 | 639,827 点、32 位姿、0 录制错误 |
| REAL-OLX-02 | 真实 OLX 重新读回 | 30 帧和 639,827 有效点，识别 XYZRGBA 16 B，颜色保留 |
| REAL-OLX-03 | 录制期间未启用相机话题 | 0 相机帧时只告警，点云回放、统计和测量正常 |
| REAL-OLX-04 | 真机测试清理 | 停止设备流并删除临时会话，不删除用户地图或修改驱动配置 |
| REAL-OLX-05 | 真实 `/odin1/cloud_raw` DTOF 录制 | intensity 转为 R=G=B 灰度，点数完整且灰度不是常量 |
| REAL-BAG-01 | 真实 2,933 条 Livox/IMU ROS2 bag | 两个话题完整索引，时长和消息数与 SQLite 一致 |
| REAL-BAG-02 | 加载对应工作空间后深度诊断 | IMU 与 Livox CustomMsg 均可解码，字段话题覆盖率 100% |
| REAL-BAG-03 | 真实雷达长间隙与抖动 | 输出具体频率、CV、最大间隙、估算缺失及修复建议 |
| REAL-BAG-04 | 真实 bag 回放、暂停、继续和终止 | 自定义类型可发布，控制状态正确，无残留进程 |
| REAL-BAG-05 | 1440×880 诊断控制台截图 | 深色层级、健康轨、表格、阈值和报告按钮清晰自然 |

## 14. 退出准则

- 自动测试覆盖每个表格中的可程序验证项；算法核心不依赖 GUI 测试。
- `git diff --check`、编译告警检查、CTest 和真实地图验收全部通过。
- 人工项目保留截图/PDF 样本与逐项结果；所有失败均修复或在测试结果中明确限制。
- README 的功能说明、操作步骤、单位/精度说明和测试命令与实现一致。
